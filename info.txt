1. create virtual env:
> python -m venv venv
> pip install -r requirements.txt

2. add a dependency
> venv\Scripts\activate.bat // needed from pure terminal only (PyCharm does it automatically)
> pip install boto3

3. run a script
> python s3.py

4. save dependencies before commit:
> pip freeze > requirements.txt